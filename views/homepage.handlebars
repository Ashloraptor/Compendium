<!-- Loop over each object in the projects array -->
{{#each plants as |plant| }}
<div class="row mb-4 plant">
    <div class="col-md-5">
        <h2>
            <a href="/plant/{{plant.id}}">{{plant.name}}</a>
        </h2>
        <p>

        </p>
    </div>
    <div class="col-md-7">
        <p>
            {{plant.description}}
        </p>
    </div>
</div>
{{/each}}